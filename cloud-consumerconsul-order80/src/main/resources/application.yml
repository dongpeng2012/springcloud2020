server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 62.234.129.71
      port: 8599
      discovery:
        service-name: ${spring.application.name}